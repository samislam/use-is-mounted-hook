{
  "name": "use-is-mounted-hook",
  "main": "./dist/index.js",
  "type": "commonjs",
  "types": "./dist/declarations/index.d.ts",
  "description": "a simple package that includes two hooks `useIsMountedRef` and `useIsMountedState` to detect if the component has been mounted on the DOM or not.",
  "prettier": "@samislam/prettier-config",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc"
  },
  "devDependencies": {
    "@samislam/prettier-config": "^1.1.1",
    "@types/bun": "latest",
    "@types/react": "^19.0.8"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "react": "^19.0.0"
  }
}
